import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-external-nav',
  imports: [RouterLink],
  templateUrl: './external-nav.html',
  styleUrl: './external-nav.css',
})
export class ExternalNav {}
